# DSTF_Diffusion
## **DSTF-Diffusion Demo**

- **temporal_stream_examples**  
  Shows how `temporal_stream` clusters different attack patterns.

- **field_stream_examples**  
  Shows how `field_stream` maps network terms to color names, and presents multi-view prompts.

- **generated_samples**  
  DSTF-Diffusion generated samples, named as `{attack_type}_{batchID}_{random_seed}.png`.

# The full code will be released soon.
